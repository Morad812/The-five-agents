# gpt-image-gen — Skill Pointer

The image generation skill lives at `.claude/skills/gpt-image-gen/SKILL.md`.

yuval invokes it (via Bash) for every image generation request.
Any other agent that needs images should do the same.
